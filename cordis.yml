# portable overlay: relative name only. --patch resolves against the profile
# directory, so cold-boot through `dshx start` / `dshx verify-boot`, not raw pnpm dsh.
- insert:
    - id: dsh-resume
      name: './lib/dsh-resume.js'
